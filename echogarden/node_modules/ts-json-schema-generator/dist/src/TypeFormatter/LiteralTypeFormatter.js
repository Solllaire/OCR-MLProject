import { LiteralType } from "../Type/LiteralType.js";
import { typeName } from "../Utils/typeName.js";
export class LiteralTypeFormatter {
    supportsType(type) {
        return type instanceof LiteralType;
    }
    getDefinition(type) {
        return {
            type: typeName(type.getValue()),
            const: type.getValue(),
        };
    }
    getChildren(type) {
        return [];
    }
}
//# sourceMappingURL=LiteralTypeFormatter.js.map